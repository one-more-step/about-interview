###JavaSrcipt
```
function test() {
    for (var i = 0; i < 3; i++) {
        var img = document.createElement('img');
        img.src = 'img' + i + '.png';
        img.onload = function () {
            alert(i)
        }
        document.body.appendChild(img);
    }
}
test();
```
问弹出几次弹窗,弹出的是多少? 